<span class="th">
	<img src="{{ image }}">
</span>
<div class="content">
	[<span>{{ imdb_rating }}</span>]
	<span class="watchlist"></span>
	<h4>{{ name }}</h4> <span>{{ year }}</span>
	<ul>
		{{#each janre }}
			<li><span class="label secondary round">{{ this }}</span></li>
		{{/each}}
	</ul>
	<footer>
		<a href="#movie/{{ id }}" class="button tiny select">details</a>
		<span class="button tiny hide">hide</span>
	</footer>
</div>